{
  "name": "smartpay-workspace",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "web",
    "mobile",
    "backend"
  ],
  "scripts": {
    "dev:backend": "npm run dev --prefix backend",
    "dev:web": "npm run dev --prefix web",
    "dev:mobile": "npm run start --prefix mobile",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:web\"",
    "dev:all": "concurrently \"npm run dev:backend\" \"npm run dev:web\" \"npm run dev:mobile\"",
    "build:backend": "npm run build --prefix backend",
    "build:web": "npm run build --prefix web",
    "build": "npm run build:backend && npm run build:web",
    "install:all": "npm install --prefix backend && npm install --prefix web --legacy-peer-deps && npm install --prefix mobile --legacy-peer-deps"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
