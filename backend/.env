PORT=5000
JWT_SECRET=smartpay_sme_secret_jwt_key_2026

# DATABASE STORAGE CONFIGURATION
# SQLite (Local Development default)
DATABASE_URL="file:./dev.db"

# PostgreSQL (Production Connection)
# To switch to PostgreSQL:
# 1. Update the 'provider' field inside backend/prisma/schema.prisma to "postgresql"
# 2. Uncomment and customize the database connection string below:
# DATABASE_URL="postgresql://postgres_user:secure_password@localhost:5432/smartpay_db?schema=public"
